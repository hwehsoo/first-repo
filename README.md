

# first-repo

gitghub와 git을 배우기 위한 첫 저장소

see [Demo] (https:// )

## Prerequisites

- python 3.12
- Pandas==1.0.0

## How to Start
...shell
$ git clone {repo addr}
$ cd {repo addr}
$ pip install -r requirements.txt
$ python main.py
...

## Installation

### github clone

'$ git clone {repo addr}'

### pip download

### Using homebrew(for MacOS)
'$ brew install {pkg name}'

## Features
- 계산하기
